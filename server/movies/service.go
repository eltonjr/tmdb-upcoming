package movies

func getMovies(name string, limit, offset int) (Collection, error) {
	// TODO return movies
	return Collection{}, nil
}

func getMovie(id int) (Movie, error) {
	// TODO return single movie
	return Movie{}, nil
}
